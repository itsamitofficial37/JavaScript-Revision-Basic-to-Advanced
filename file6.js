// String Indexing 

let firstName = "AMIT";

//  A     M     I    T
//  0     1     2    3       index no.

//  if we want to print particular index of string  we should use variable namw with square bracket 

let index = firstName[3];
console.log(index);

//  string length 

let len = firstName.length
console.log(len);

// if we add space in string length  count the space also 


// last index 

console.log(firstName.length-1);